(executable
  (name     dnsserver)
  (modules dnsserver)
  (libraries mpst_plug dns ipaddr.unix lwt.unix dnshelper))

(library
  (name dnshelper)
  (modules dnshelper)
  (libraries mpst_plug dns ipaddr.unix lwt.unix))
