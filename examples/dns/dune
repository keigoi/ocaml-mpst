(executable
  (name     dnsserver)
  (modules dnsserver)
  (libraries mpst_apibase dns-lwt-unix lwt.unix dnshelper))

(library
  (name dnshelper)
  (modules dnshelper)
  (libraries mpst_apibase dns-lwt-unix lwt.unix))
